功能：
解决 qt5.5 linuxfb 下无法旋转程序的问题。
以及根据旋转补丁包修改源码后导致透明失效的问题。
(旋转补丁包可以网络搜索获取, 这里的都为改好的了)

使能旋转透明后不黑底
使能程序旋转
实际使用的版本 qt.5.5.1 需要修改 platformsupport (使能旋转透明)  
和 plugins (使能旋转) 两个文件夹下的文件

实际使用的版本 qt5.9.9 则只需要修改 plugins 文件夹下的文件即可

使用 qt.5.5.1 的时候发现了报错 "网络被禁用"，好像是一个 bug，虽
然被修复了，但是既然要修改源码，干脆直接用新的版本好了 
      
          


date: 2021-01-29
author: hehj

